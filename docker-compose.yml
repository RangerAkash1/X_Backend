version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: appdb
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
  backend:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db
